# Example Data

`cboe_option_chain_sample.csv` is a small synthetic provider-style option-chain
sample for examples only.

It is not downloaded vendor data and does not represent real historical market
events. The sample intentionally includes a mix of usable quotes and deliberate
quality issues such as missing bid, crossed quote, locked quote, wide spread,
low open interest, and static-arbitrage inconsistencies.
